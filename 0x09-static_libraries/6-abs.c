#include "main.h"
/**
 * _abs - function that compute the absolute value of an integer
 * @n: int type number to check
 * Return: absolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);

}
