#include "main.h"

/**
 * swap_int - sap the function name
 * @a: first parameter of the function
 * @b: second parameter of the function
 */

void swap_int(int *a, int *b)
{
int c;
c = *a;
*a = *b;
*b = c;

}
